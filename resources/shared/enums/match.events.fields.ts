export enum MatchEventFields {
  match_id = "match_id",
  timestamp = "timestamp",
  team = "team",
  opponent = "opponent",
  event_type = "event_type",
  event_details = "event_details",
}

export enum MatchEventDetailsFields {
  player = "player",
  assist = "assist",
  goal_type = "goal_type",
  minute = "minute",
  video_url = "video_url",
}
